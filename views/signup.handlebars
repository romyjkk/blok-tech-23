{{> header}}

<section class="container">

    <h1 class="title">Sign up</h1>

    <form id="signup" method="POST" action="/signup">

        <fieldset class="form-container">
            <label for="username">Username</label>
            <input type="text" id="username" name="username" minlength="3" maxlength="20" required/>

            <label for="email">Email</label>
            <input type="email" id="email" name="email" pattern="^[-!#$%&'*+/0-9=?A-Z^_a-z{|}~](.?[-!#$%&'*+/0-9=?A-Z^_a-z`{|}~])*@[a-zA-Z0-9](-*.?[a-zA-Z0-9])*.[a-zA-Z](-?[a-zA-Z0-9])+$" required/>

            <label for="age">Age</label>
            <input type="number" id="age" name="age" min="12" max="120" required/>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" minlength="8" maxlength="50" pattern="^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{8,50}$" required/>
                
                {{#if error}}
                    <div class="error-container">
                        <p class="error-message">{{ error }}</p>
                    </div>
                {{/if}}
        </fieldset>

        <input type="submit" id="submit" class="login-button" value="Sign up!" />

    </form>

<a href="/login" class="italic">Log in instead</a>
</section>